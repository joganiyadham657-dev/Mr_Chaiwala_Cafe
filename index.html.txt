<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    <title>Mr. Chaiwala & Cafe</title>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    
    <style>
        :root {
            --primary: #2c1b0e; 
            --accent: #e6b325;  
            --light: #fdfaf5;   
            --white: #ffffff;
        }

        body { 
            padding-top: env(safe-area-inset-top); 
            background-color: var(--light);
            margin: 0;
            overflow-x: hidden;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Poppins', sans-serif; }

        /* --- 1. SPLASH SCREEN --- */
        #splash-screen {
            position: fixed;
            top: 0; left: 0;
            width: 100%; height: 100%;
            background: linear-gradient(rgba(44, 27, 14, 0.9), rgba(44, 27, 14, 0.9)), url('https://images.unsplash.com/photo-1507133750040-4a8f57021571?auto=format&fit=crop&w=800&q=80');
            background-size: cover;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            z-index: 9999;
            transition: transform 0.8s cubic-bezier(0.77, 0, 0.175, 1);
        }

        .welcome-text {
            color: var(--accent);
            font-family: 'Playfair Display', serif;
            font-size: 2.5rem;
            text-align: center;
            padding: 0 20px;
            margin-top: -50px; /* Thoda upar shift kiya taaki niche jagah mile */
            opacity: 0;
            animation: fadeInUp 1.2s forwards 0.5s;
        }

        .splash-subtext {
            color: var(--white);
            margin-bottom: 40px;
            letter-spacing: 3px;
            font-size: 0.9rem;
            opacity: 0;
            animation: fadeIn 1s forwards 1.5s;
        }

        .enter-btn {
            padding: 18px 45px;
            background: var(--accent);
            color: var(--primary);
            border: none;
            border-radius: 50px;
            font-weight: bold;
            font-size: 1rem;
            cursor: pointer;
            box-shadow: 0 10px 20px rgba(0,0,0,0.3);
            opacity: 0;
            animation: fadeIn 1s forwards 2s;
            text-decoration: none; 
            display: inline-block; 
        }

        /* NEW: Download Section Styling */
        .download-container {
            position: absolute;
            bottom: 40px;
            text-align: center;
            opacity: 0;
            animation: fadeIn 1s forwards 2.5s; /* Ye sabse last me fade in hoga */
        }

        .download-container p {
            color: rgba(255, 255, 255, 0.8);
            font-size: 0.85rem;
            margin-bottom: 10px;
            letter-spacing: 1px;
        }

        .download-btn {
            padding: 12px 25px; 
            background-color: #28a745; 
            color: white; 
            text-decoration: none; 
            border-radius: 25px; 
            font-weight: bold;
            font-size: 0.9rem;
            display: inline-block;
            box-shadow: 0 5px 15px rgba(0,0,0,0.3);
            transition: 0.3s;
        }

        .download-btn:active {
            transform: scale(0.95);
        }

        @keyframes fadeInUp { to { opacity: 1; transform: translateY(0); } }
        @keyframes fadeIn { to { opacity: 1; } }

        /* --- 2. HEADER --- */
        header {
            background: var(--primary);
            color: var(--accent);
            padding: 55px 20px 20px; 
            text-align: center;
            font-family: 'Playfair Display', serif;
            font-size: 1.5rem;
            position: sticky;
            top: 0;
            z-index: 100;
            box-shadow: 0 4px 15px rgba(0,0,0,0.3);
        }

        /* --- 3. PAGES --- */
        .page { padding: 20px; padding-bottom: 120px; display: none; animation: slideIn 0.4s ease-out; }
        .active-page { display: block; }

        @keyframes slideIn { from { opacity: 0; transform: translateY(15px); } to { opacity: 1; transform: translateY(0); } }

        /* HERO CARD IMAGE */
        .hero-card {
            background: linear-gradient(rgba(0,0,0,0.4), rgba(0,0,0,0.4)), url('https://www.shutterstock.com/image-photo/beautiful-natural-wooden-table-coffee-600nw-2636929395.jpg');
            height: 250px;
            border-radius: 30px;
            background-size: cover;
            background-position: center;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            color: white;
            margin-bottom: 25px;
            text-align: center;
            box-shadow: 0 15px 30px rgba(0,0,0,0.2);
            border: 2px solid rgba(230, 179, 37, 0.3);
        }

        .category-title { color: var(--primary); margin: 25px 0 15px; border-left: 5px solid var(--accent); padding-left: 12px; font-weight: 600; font-size: 1.2rem; }
        
        .food-item {
            background: white;
            display: flex;
            padding: 12px;
            border-radius: 20px;
            margin-bottom: 15px;
            box-shadow: 0 8px 20px rgba(0,0,0,0.06);
            align-items: center;
            transition: 0.3s;
        }
        .food-item:active { transform: scale(0.97); }

        .food-item img { 
            width: 85px; height: 85px; 
            border-radius: 15px; 
            object-fit: cover; 
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
        }

        .food-details { flex: 1; padding-left: 15px; }
        .food-details h4 { font-size: 1rem; color: #333; font-weight: 600; }
        .price { color: #27ae60; font-weight: 700; margin-top: 5px; font-size: 1.1rem; }

        .about-box { 
            background: var(--primary); 
            background-image: linear-gradient(rgba(44, 27, 14, 0.8), rgba(44, 27, 14, 0.8)), url('https://images.unsplash.com/photo-1495474472287-4d71bcdd2085?auto=format&fit=crop&w=500&q=60');
            background-size: cover;
            color: white; 
            padding: 40px 20px; 
            border-radius: 30px; 
            text-align: center; 
        }
        
        .contact-pill { background: rgba(255,255,255,0.15); backdrop-filter: blur(5px); padding: 12px; border-radius: 15px; margin: 10px 0; font-size: 0.9rem; border: 1px solid rgba(255,255,255,0.1); }

        /* --- 4. NAVIGATION BAR --- */
        .nav-bar {
            position: fixed;
            bottom: 25px;
            left: 5%;
            width: 90%;
            background: rgba(44, 27, 14, 0.95);
            backdrop-filter: blur(10px);
            display: flex;
            justify-content: space-around;
            padding: 15px 0;
            border-radius: 35px;
            box-shadow: 0 15px 35px rgba(0,0,0,0.4);
            z-index: 1000;
        }

        .nav-item { text-align: center; color: #8d7d71; cursor: pointer; font-size: 0.75rem; transition: 0.4s; }
        .nav-item.active { color: var(--accent); transform: translateY(-5px); }
        .nav-item i { font-size: 1.4rem; display: block; margin-bottom: 3px; }

    </style>
</head>
<body>

    <div id="splash-screen">
        <h1 class="welcome-text">Mr. Chaiwala & Cafe</h1>
        <p class="splash-subtext">THE ULTIMATE CAFE VIBE</p>
        <button class="enter-btn" onclick="startApp()">Taste the Magic <i class="fas fa-magic"></i></button>

        <div class="download-container">
            <p>App download from here</p>
            <a href="/storage/emulated/0/Android/media/com.whatsapp.w4b/WhatsApp Business/Media/WhatsApp Business Documents/app-release.apk" download class="download-btn">
                <i class="fas fa-download"></i> Download APK
            </a>
        </div>
    </div>

    <div id="main-app" style="display:none;">
        <header>Mr. Chaiwala & Cafe</header>

        <div id="home" class="page active-page">
            <div class="hero-card">
                <h2 style="font-family: 'Playfair Display'; font-size: 2rem; text-shadow: 2px 2px 10px rgba(0,0,0,0.5);">Freshly Brewed</h2>
                <p style="font-size: 0.9rem; margin-top: 5px; font-weight: 300;">Experience the best chai in city</p>
            </div>
            
            <h3 style="margin-bottom: 15px; font-size: 1.3rem;">Must Try Today </h3>
            
            <div class="food-item">
                <img src="https://cdn.shopify.com/s/files/1/0687/6050/2545/files/ginger-slices-jagger.webp?v=1688973432" alt="Kulhad Chai">
                <div class="food-details">
                    <h4>Special Desi Ginger Chai</h4>
                    <p class="price">₹20</p>
                </div>
                <i class="fas fa-heart" style="color: #e74c3c; margin-right: 10px;"></i>
            </div>
        </div> 

        <div id="menu" class="page">
            <h3 class="category-title">Chai & Coffee</h3>
            <div class="food-item">
                <img src="https://images.unsplash.com/photo-1561336313-0bd5e0b27ec8?auto=format&fit=crop&w=200&q=60" alt="Masala Chai">
                <div class="food-details"><h4>Masala Chai</h4><p class="price">₹20</p></div>
            </div>
            <div class="food-item">
                <img src="https://images.unsplash.com/photo-1461023058943-07fcbe16d735?auto=format&fit=crop&w=200&q=60" alt="Cold Coffee">
                <div class="food-details"><h4>Iced Cold Coffee</h4><p class="price">₹75</p></div>
            </div>

            <h3 class="category-title">Snacks & Fast Food</h3>
            <div class="food-item">
                <img src="https://images.unsplash.com/photo-1550547660-d9450f859349?auto=format&fit=crop&w=200&q=60" alt="Burger">
                <div class="food-details"><h4>Aloo Tikki Burger</h4><p class="price">₹50</p></div>
            </div>
            <div class="food-item">
                <img src="https://images.unsplash.com/photo-1585109649139-366815a0d713?auto=format&fit=crop&w=200&q=60" alt="Sandwich">
                <div class="food-details"><h4>Grilled Cheese Sandwich</h4><p class="price">₹90</p></div>
            </div>
        </div>

        <div id="about" class="page">
            <div class="about-box">
                <div style="font-size: 3rem; color: var(--accent); margin-bottom: 10px;">☕</div>
                <h2>Mr. Chaiwala & Cafe</h2>
                <p style="font-size: 0.8rem; opacity: 0.8; margin-bottom: 20px;">Swaad jo Dil tak Jaye!</p>
                
                <div class="contact-pill"><i class="fas fa-phone-alt"></i> +91 63788 51058</div>
                <div class="contact-pill"><i class="fas fa-map-marker-alt"></i> Near Railway Station, Lakheri, 323603</div>
                <div class="contact-pill"><i class="fas fa-clock"></i> 09:00 AM - 10:30 PM</div>
                
                <div style="margin-top: 30px;">
                    <button class="enter-btn" style="padding: 10px 25px; opacity: 1; animation: none; color: #2c1b0e; cursor: default;">
                        <i class="fas fa-phone-alt"></i> Order on Call
                    </button>
                </div>
            </div>
        </div>

        <div class="nav-bar">
            <div class="nav-item active" onclick="showPage('home', this)">
                <i class="fas fa-mug-hot"></i>
                <span>Home</span>
            </div>
            <div class="nav-item" onclick="showPage('menu', this)">
                <i class="fas fa-utensils"></i>
                <span>Menu</span>
            </div>
            <div class="nav-item" onclick="showPage('about', this)">
                <i class="fas fa-info-circle"></i>
                <span>About</span>
            </div>
        </div>
    </div>

    <script>
        function startApp() {
            const splash = document.getElementById('splash-screen');
            const mainApp = document.getElementById('main-app');
            splash.style.transform = "translateY(-100%)";
            mainApp.style.display = "block";
            window.scrollTo(0,0);
        }

        function showPage(pageId, element) {
            const pages = document.querySelectorAll('.page');
            pages.forEach(p => p.classList.remove('active-page'));
            document.getElementById(pageId).classList.add('active-page');

            const navItems = document.querySelectorAll('.nav-item');
            navItems.forEach(item => item.classList.remove('active'));
            element.classList.add('active');
            window.scrollTo(0,0);
        }
    </script>
</body>
</html>

